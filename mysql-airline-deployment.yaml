apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-airline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-airline
  template:
    metadata:
      labels:
        app: mysql-airline
    spec:
      containers:
        - name: mysql
          image: mysql:latest
          ports:
            - containerPort: 3307
              hostPort: 3307
          volumeMounts:
            - name: mysql-airline-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-airline-storage
          persistentVolumeClaim:
            claimName: mysql-airline-pvc
